name: Build flake outputs

on:
  push:
    branches:
      - main
      - next
      - build/gha
  # pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.branch }}
  cancel-in-progress: true

jobs:
  nix:
    runs-on: ${{ matrix.system }}
    strategy:
      matrix:
        system:
          - aaarch64-darwin
          - aaarch64-linux
          - x86_64-linux
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          nix-fast-build \
            --eval-workers 10 \
            --eval-max-memory-size 2048 \
            --no-nom \
            --systems "${{ matrix.system }}" \
            --flake .#hydraJobs
      - name: Add gc-root
        run: |
          set -x
          gc-root add --profile "${{ github.repo }}/${{ github.branch }}" --generation "${{ github.ref }}" ./result*
